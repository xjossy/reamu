class DebugConfig {
  // Set to false for production builds
  static const bool debugMode = true;
  
  // Debug features
  static const bool showDebugButton = debugMode;
  static const bool debugLogs = debugMode;
  static const bool enableDebugActions = debugMode;
}
